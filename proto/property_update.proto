syntax = "proto3";
package google.gink;
import "muid.proto";
import "value.proto";

message PropertyUpdate {
    Muid updates_what = 1; // specifies the thing being updated
    Muid property_type = 2; // what property is being updated
    Value value = 3; // value it is being set to
}
