syntax = "proto3";
package gink;
import "proto/muid.proto";

/**
    Indicates that a database object should be removed.
*/
message Detachment {
    Muid node = 1;
    bool purge = 2;  // when set to true, remove described node from history as well
}
